# Core: everything the models and metrics need.
numpy>=1.26
scipy>=1.11
pandas>=2.0
scikit-learn>=1.3
lightgbm>=4.0
pyarrow>=14.0

# Storage. The document is right that the data is small: DuckDB over
# Parquet handles years of daily snapshots on a laptop, and reaching for a
# warehouse should need a reason.
duckdb>=0.10
polars>=0.20

# Tests. hypothesis is not optional -- the property test in
# tests/test_leakage.py is what kills the leakage class.
pytest>=7.4
hypothesis>=6.90
